# 本番環境用データベース設定
DB_HOST=mysql307.phy.lolipop.lan # 例: localhost や データベースサーバーのホスト名
DB_PORT=3306 # 例: 3306
DB_NAME=LAA1530328-wedding # 例: wedding_db
DB_USER=LAA1530328 # 例: db_user
DB_PASSWORD=your_database_password # <- ここにデータベースパスワードを入力

# アプリケーション環境設定
APP_ENV=production # local, development, production など
APP_DEBUG=false # デバッグモード (true or false)

# アプリケーションの秘密鍵（セキュリティのため、ランダムな文字列を生成して設定してください）
APP_SECRET_KEY=your_strong_random_secret_key # 例: openssl rand -base64 32 で生成

# サイト設定 (config.php のデフォルト値を上書きする場合)
# SITE_URL=http://localhost/sopranohaction/
# SITE_NAME="私たちの結婚式"
# SITE_EMAIL=info@example.com

# メール設定 (PHPMailer)
SMTP_HOST=smtp.example.com # 例: smtp.lolipop.jp
SMTP_PORT=587 # 例: 465 (SSL), 587 (TLS)
SMTP_USERNAME=user@example.com # 例: info-wedding@sopranohaction.fun
SMTP_PASSWORD=your_smtp_password # <- ここにSMTPパスワードを入力
SMTP_SECURE=tls # 例: tls, ssl
FROM_EMAIL=noreply@example.com # 送信元メールアドレス
FROM_NAME="私たちの結婚式" # 送信元名

# Google reCAPTCHA (index.php で使用)
# RECAPTCHA_SITE_KEY=your_recaptcha_site_key
# RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key

# メールデバッグ (true or false)
# MAIL_DEBUG=false # メール送信の詳細ログを出力する場合 true

# QRコード設定 (オプション - qr_helper.php で使用される可能性)
# QR_ERROR_CORRECTION_LEVEL=L # L, M, Q, H
# QR_SIZE=300
# QR_MARGIN=4
